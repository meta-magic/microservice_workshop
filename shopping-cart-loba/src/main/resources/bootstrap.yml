spring:
  profiles:
    active: dev_env
  application:
    name: shoppingcartlobaservice
  cloud:
    config:
      uri: http://localhost:7001
      username: config
      password: config
      
      
